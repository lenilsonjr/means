# means

Books that survive an audit: a local double-entry accounting engine across entities and currencies. Rust workspace: means-core (ledger, valuation, hash-chained entries), means-proto (the gRPC contract), means-server, means-tui. SQLite file store. See README.md and docs/.

## How work arrives
Your item is the brief you were started with. The tracker lives outside this working copy: do not run bd, and do not file issues. Report in your reply.

## Before you report
Run what hooks/ship runs on this tree: cargo fmt, cargo clippy with warnings denied, cargo test. A documentation-only change skips them when the brief says so. Leave the tree clean and committed.

## Prose
All prose in ASD-STE100 Simplified Technical English: comments, commit messages, replies. Short sentences. Active voice. One instruction per sentence.
